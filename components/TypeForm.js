import { PopupButton } from '@typeform/embed-react';

export const TypeForm = ({ children }) => {
	return (
		<PopupButton id='https://zgtxzilajaj.typeform.com/to/xLzNeQVK'>
			{children}
		</PopupButton>
	);
};
