import { PopupButton } from '@typeform/embed-react';

export const TypeForm = ({ children }) => {
	return (
		<PopupButton id='https://fernando160641.typeform.com/to/mkKt2NMg'>
			{children}
		</PopupButton>
	);
};
